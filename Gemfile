source "https://rubygems.org"

# Jekyll static site generator. Pinned to 4.x for Vercel builds
# (we no longer depend on the GitHub Pages gem).
gem "jekyll", "~> 4.3"

# Required to run `jekyll serve` locally on Ruby 3.0+
gem "webrick", "~> 1.8"
